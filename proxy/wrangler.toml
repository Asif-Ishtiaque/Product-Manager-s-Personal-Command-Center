name = "tide-proxy"
main = "worker.js"
compatibility_date = "2026-01-01"

# Local dev reads secrets from .dev.vars (gitignored).
# Production secrets are set with: wrangler secret put NAME

[[d1_databases]]
binding = "DB"
database_name = "tide_db"
database_id = "local_db"

